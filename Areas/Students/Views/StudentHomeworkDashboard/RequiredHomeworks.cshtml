@model List<QdratNew.ViewModels.Homework.HomeworkListVm>

<div class="container mt-4">

    <!-- 🔹 الكروت -->
    <div class="row g-4 mb-4 text-center">
        <div class="col-md-6 col-12">
            <div class="card bg-qdrat-light shadow-sm border-0 rounded-3 h-100">
                <div class="card-body">
                    <h6 class="text-muted">عدد الواجبات المطلوبة</h6>
                    <h3 class="fw-bold text-primary">@ViewBag.TotalRequired</h3>
                </div>
            </div>
        </div>
        <div class="col-md-6 col-12">
            <div class="card bg-qdrat-light shadow-sm border-0 rounded-3 h-100">
                <div class="card-body">
                    <h6 class="text-muted">المعدل الحالي</h6>
                    <h3 class="fw-bold text-success">@ViewBag.PendingScore %</h3>
                </div>
            </div>
        </div>
    </div>

    <!-- 🔹 الجدول -->
    <div class="card shadow-sm rounded-3">
        <div class="card-header bg-qdrat text-white fw-bold text-center">
            🕒 قائمة الواجبات المطلوبة
        </div>
        <div class="card-body p-0">
            <table id="requiredHomeworksTable" class="table table-bordered table-hover align-middle text-center mb-0 modern-table">
                <thead class="table-light">
                    <tr>
                        <th>اسم الواجب</th>
                        <th class="d-none d-md-table-cell">تاريخ الإنشاء</th>
                        <th class="d-none d-md-table-cell">الدرجة الحالية</th>
                        <th>الإجراء</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var hw in Model)
                    {
                        <tr>
                            <!-- ✅ اسم الواجب -->
                            <td class="fw-bold">@hw.Title</td>

                            <!-- ✅ الأعمدة الثانوية -->
                            <td data-label="تاريخ الإنشاء" class="d-none d-md-table-cell">@hw.CreatedAt.ToString("yyyy-MM-dd")</td>
                            <td data-label="الدرجة الحالية" class="d-none d-md-table-cell">
                                <span class="badge bg-secondary">قيد الحل</span>
                            </td>

                            <!-- ✅ زر بدء الحل -->
                            <td>
                                <a asp-action="Start" asp-route-homeworkSetId="@hw.HomeworkSetId"
                                   class="btn btn-sm btn-success w-100 rounded-pill">
                                    <i class="fa-solid fa-play me-1"></i> بدء الحل
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<!-- ✅ سكربت DataTable -->
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.datatables.net/1.13.6/js/jquery.dataTables.min.js"></script>
<script src="https://cdn.datatables.net/1.13.6/js/dataTables.bootstrap5.min.js"></script>

<script>
    $(document).ready(function () {
        $('#requiredHomeworksTable').DataTable({
            language: {
                url: "//cdn.datatables.net/plug-ins/1.13.6/i18n/ar.json"
            },
            pageLength: 10,
            lengthMenu: [5, 10, 20, 50],
            order: [[2, "desc"]],
            responsive: true
        });
    });
</script>

<!-- 🎨 CSS -->
<style>
    .bg-qdrat {
        background-color: #1ba098 !important;
    }

    .bg-qdrat-light {
        background-color: #f1fafa !important;
    }

    table th, table td {
        vertical-align: middle !important;
    }

    .table-hover tbody tr:hover {
        background-color: #f9fbff;
        transition: 0.3s ease;
    }

    .btn-sm {
        padding: 6px 14px;
        font-size: 0.85rem;
        transition: 0.3s ease;
    }

        .btn-sm:hover {
            transform: scale(1.05);
        }

    /* 📱 تصميم الموبايل الذكي */
    @@media (max-width: 767px) {
        table thead

    {
        display: none;
    }

    table, tbody, tr, td {
        display: block;
        width: 100%;
        text-align: right;
    }

    tr {
        margin-bottom: 1rem;
        background: #fff;
        border-radius: 1rem;
        box-shadow: 0 2px 6px rgba(0,0,0,0.08);
        padding: 0.8rem;
    }

    td {
        padding: 0.6rem 1rem;
        position: relative;
        border: none;
    }

        /* ✅ الأعمدة الثانوية فقط يظهر عنوانها */
        td[data-label]::before {
            content: attr(data-label);
            position: absolute;
            left: 0.8rem;
            top: 0.6rem;
            font-weight: 600;
            color: #1ba098;
            font-size: 0.9rem;
        }

    /* 🔹 أول خلية (اسم الواجب) */
    tr td:first-child {
        font-weight: 700;
        font-size: 1rem;
        color: #1ba098;
        margin-bottom: 0.4rem;
    }

    /* 🔹 خلية الأزرار */
    tr td:last-child {
        margin-top: 0.5rem;
        display: flex;
        flex-direction: column;
        gap: 0.4rem;
    }

    .btn-sm {
        font-size: 0.9rem;
        padding: 0.6rem;
    }

    }
</style>
