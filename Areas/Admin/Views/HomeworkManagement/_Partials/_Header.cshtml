@using QdratNew.ViewModels.Homework
@model HomeworkAnalyticsViewModel

<div class="card-custom fade-in hover-lift">

    <h2 class="fw-bold text-primary mb-3">
        @Model.StudentName
    </h2>

    <div class="d-flex flex-wrap gap-4 text-muted">

        <div>🏫 @Model.BatchName</div>

        <div>📘 واجب رقم: @Model.HomeworkSetId</div>

        <div class="badge bg-success px-3 py-2">
            مكتمل
        </div>

    </div>

</div>