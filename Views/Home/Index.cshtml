@model QdratNew.ViewModels.Public.HomePage.HomePageContentViewModel
@{
    ViewData["Title"] = "الصفحة الرئيسية";
    Layout = "~/Views/Shared/_FrontendLayout.cshtml";
}

@await Html.PartialAsync("MainPageSections/_HeroSection", Model)






@await Html.PartialAsync("MainPageSections/_SmartStatsSection")



@await Html.PartialAsync("MainPageSections/_ProgramsAndCoursesSection", Model)
@await Html.PartialAsync("MainPageSections/_ProfessionalCertificatesSection")
@await Html.PartialAsync("MainPageSections/_CourseCollectionsSection")
@await Html.PartialAsync("MainPageSections/_AboutUsSection", Model)

@await Html.PartialAsync("MainPageSections/_VisionMissionSection", Model)

@* @await Html.PartialAsync("MainPageSections/_SuccessStoriesSection") *@

@await Html.PartialAsync("MainPageSections/_PartnersSliderSection", Model)

@await Html.PartialAsync("MainPageSections/_PlatformGoalsSection", Model)

@await Html.PartialAsync("MainPageSections/_SaudiVisionSection", Model)



@await Html.PartialAsync("MainPageSections/_AIAnalysisSection")


@await Html.PartialAsync("MainPageSections/_RemedialPlanFlowSection")
@* @await Html.PartialAsync("MainPageSections/_JourneyTimelineSection") *@
@await Html.PartialAsync("MainPageSections/_PartnersSection", Model)
 @await Html.PartialAsync("MainPageSections/_ContactUsSection")




<script>
    document.addEventListener("DOMContentLoaded", () => {
        const counters = document.querySelectorAll(".counter");

        counters.forEach(counter => {
            const target = parseInt(counter.getAttribute("data-count"));
            let count = 0;
            const step = Math.ceil(target / 80);

            const update = () => {
                count += step;
                if (count < target) {
                    counter.textContent = Math.floor(count);
                    requestAnimationFrame(update);
                } else {
                    counter.textContent = target;
                }
            };

            update();
        });
    });
</script>
