@* Sprint 13 (MSE-G / G5): تصميم qlb- المشترك لشاشات مراجعة الأسئلة/الإجابات — مستخرج من
   Areas/Students/Views/StudentHomeworkResults/Review.cshtml ليُستخدم في أكثر من شاشة/Area بدل التكرار.
   Partial بلا Model — CSS ثابت فقط، آمن للاستدعاء من Students أو Admin. *@

<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet" />

<style>
    :root {
        --q-primary: #4f46e5;
        --q-primary-lt: #ede9fe;
        --q-success: #16a34a;
        --q-success-lt: #dcfce7;
        --q-danger: #dc2626;
        --q-danger-lt: #fee2e2;
        --q-warning: #d97706;
        --q-warning-lt: #fef3c7;
        --q-info: #0891b2;
        --q-info-lt: #cffafe;
        --q-skip: #6b7280;
        --q-skip-lt: #f3f4f6;
        --q-surface: #ffffff;
        --q-bg: #f5f3ff;
        --q-border: #e5e7eb;
        --q-text: #1e1b4b;
        --q-muted: #6b7280;
        --q-radius: 16px;
        --q-shadow: 0 2px 12px rgba(79,70,229,.08);
        --q-shadow-lg: 0 6px 32px rgba(79,70,229,.13);
    }

    body {
        background: var(--q-bg) !important;
    }

    .rtl-mode {
        direction: rtl;
        text-align: right;
    }

    .ltr-mode {
        direction: ltr;
        text-align: left;
    }

    .qlb-page {
        font-family: 'Cairo', 'Tajawal', 'Segoe UI', sans-serif;
        background: var(--q-bg);
        padding: 1.5rem 1rem 3rem;
        min-height: 100vh;
        font-size: .93rem;
    }

    .qlb-shell {
        max-width: 1320px;
        margin: 0 auto;
    }

    .qlb-hero {
        background: linear-gradient(135deg, #4f46e5 0%, #7c3aed 55%, #a855f7 100%);
        border-radius: var(--q-radius);
        padding: 1.4rem 1.5rem;
        color: #fff;
        margin-bottom: 1.25rem;
        box-shadow: var(--q-shadow-lg);
        position: relative;
        overflow: hidden;
    }

        .qlb-hero::before {
            content: '';
            position: absolute;
            inset: 0;
            background: radial-gradient(circle at 85% 50%, rgba(255,255,255,.10) 0%, transparent 60%);
            pointer-events: none;
        }

    .qlb-hero-content {
        position: relative;
        z-index: 1;
    }

    .qlb-hero-icon {
        width: 42px;
        height: 42px;
        background: rgba(255,255,255,.18);
        border: 1px solid rgba(255,255,255,.25);
        border-radius: 10px;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.2rem;
        margin-bottom: .55rem;
    }

    .qlb-hero h1 {
        font-size: clamp(1.1rem, 2.2vw, 1.45rem);
        font-weight: 800;
        margin: 0 0 .3rem;
        line-height: 1.35;
    }

    .qlb-hero-subtitle {
        font-size: .87rem;
        opacity: .92;
        margin: 0;
        line-height: 1.65;
    }

    .qlb-hero-meta {
        display: flex;
        flex-wrap: wrap;
        gap: .5rem;
        margin-top: .7rem;
    }

    .qlb-hero-pill {
        background: rgba(255,255,255,.15);
        border: 1px solid rgba(255,255,255,.22);
        border-radius: 8px;
        padding: .26rem .7rem;
        font-size: .76rem;
        font-weight: 700;
        display: inline-flex;
        align-items: center;
        gap: .35rem;
        white-space: nowrap;
    }

    .qlb-stats {
        display: grid;
        grid-template-columns: repeat(4, 1fr);
        gap: .75rem;
        margin-bottom: 1.25rem;
    }

    .qlb-stat {
        background: var(--q-surface);
        border-radius: var(--q-radius);
        padding: .8rem .85rem;
        box-shadow: var(--q-shadow);
        border: 1px solid var(--q-border);
        display: flex;
        align-items: center;
        gap: .65rem;
        transition: transform .2s, box-shadow .2s;
    }

        .qlb-stat:hover {
            transform: translateY(-2px);
            box-shadow: var(--q-shadow-lg);
        }

    .qlb-stat-ico {
        width: 38px;
        height: 38px;
        border-radius: 10px;
        flex-shrink: 0;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 1.05rem;
    }

    .ico-blue {
        background: var(--q-primary-lt);
        color: var(--q-primary);
    }

    .ico-green {
        background: var(--q-success-lt);
        color: var(--q-success);
    }

    .ico-red {
        background: var(--q-danger-lt);
        color: var(--q-danger);
    }

    .ico-amber {
        background: var(--q-warning-lt);
        color: var(--q-warning);
    }

    .qlb-stat-content {
        flex: 1;
        min-width: 0;
    }

    .qlb-stat-lbl {
        font-size: .72rem;
        color: var(--q-muted);
        font-weight: 700;
        margin-bottom: .18rem;
    }

    .qlb-stat-val {
        font-size: 1.25rem;
        font-weight: 800;
        color: var(--q-text);
        line-height: 1.2;
        word-break: break-word;
    }

    .qlb-stat-bar {
        height: 4px;
        background: var(--q-border);
        border-radius: 99px;
        margin-top: .4rem;
        overflow: hidden;
    }

    .qlb-stat-fill {
        height: 100%;
        border-radius: 99px;
        transition: width .8s ease;
    }

    .fill-g {
        background: var(--q-success);
    }

    .fill-r {
        background: var(--q-danger);
    }

    .fill-a {
        background: var(--q-warning);
    }

    .fill-s {
        background: var(--q-skip);
    }

    .qlb-section-head {
        background: linear-gradient(90deg, var(--q-primary-lt), #ffffff);
        border: 1px solid var(--q-border);
        border-right: 4px solid var(--q-primary);
        border-radius: var(--q-radius);
        padding: .7rem .9rem;
        margin-bottom: .85rem;
        box-shadow: var(--q-shadow);
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: .7rem;
        flex-wrap: wrap;
    }

    .ltr-mode .qlb-section-head {
        border-right: 1px solid var(--q-border);
        border-left: 5px solid var(--q-primary);
    }

    .qlb-section-title {
        margin: 0;
        color: var(--q-text);
        font-size: .93rem;
        font-weight: 800;
        display: flex;
        align-items: center;
        gap: .4rem;
    }

    .qlb-section-count {
        background: var(--q-primary);
        color: #fff;
        border-radius: 99px;
        padding: .2rem .65rem;
        font-size: .72rem;
        font-weight: 800;
        display: inline-flex;
        align-items: center;
        gap: .28rem;
    }

    .qlb-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: .9rem;
    }

    .qlb-card {
        background: var(--q-surface);
        border-radius: var(--q-radius);
        box-shadow: var(--q-shadow);
        border: 1px solid var(--q-border);
        overflow: hidden;
        display: flex;
        flex-direction: column;
        transition: box-shadow .2s, transform .2s;
        min-height: 100%;
    }

        .qlb-card:hover {
            box-shadow: var(--q-shadow-lg);
            transform: translateY(-2px);
        }

    .qlb-card-head {
        background: linear-gradient(90deg, var(--q-primary-lt), #f5f3ff);
        border-bottom: 1px solid var(--q-border);
        padding: .55rem .8rem;
        display: flex;
        align-items: center;
        gap: .5rem;
        flex-wrap: wrap;
    }

    .qlb-qnum {
        min-width: 26px;
        height: 26px;
        border-radius: 7px;
        background: var(--q-primary);
        color: #fff;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        font-size: .78rem;
        font-weight: 800;
        flex-shrink: 0;
        padding: 0 .4rem;
    }

    .qlb-result-badge {
        font-size: .72rem;
        font-weight: 800;
        padding: .18rem .6rem;
        border-radius: 99px;
        display: inline-flex;
        align-items: center;
        gap: .3rem;
    }

    .result-correct {
        background: var(--q-success-lt);
        color: var(--q-success);
        border: 1px solid #86efac;
    }

    .result-wrong {
        background: var(--q-danger-lt);
        color: var(--q-danger);
        border: 1px solid #fecaca;
    }

    .result-skip {
        background: var(--q-skip-lt);
        color: var(--q-skip);
        border: 1px solid #d1d5db;
    }

    .qlb-time-chip {
        display: inline-flex;
        align-items: center;
        gap: .3rem;
        font-size: .7rem;
        color: var(--q-muted);
        font-weight: 800;
        margin-right: auto;
    }

    .ltr-mode .qlb-time-chip {
        margin-right: 0;
        margin-left: auto;
    }

    .qlb-card-body {
        padding: .8rem .85rem;
        flex: 1;
        display: flex;
        flex-direction: column;
        gap: .65rem;
    }

    .qlb-passage {
        background: #fafaf5;
        border: 1px solid #e8e4cf;
        border-radius: 10px;
        padding: .6rem .75rem;
        border-right: 3px solid var(--q-warning);
    }

    .ltr-mode .qlb-passage {
        border-right: 1px solid #e8e4cf;
        border-left: 3px solid var(--q-warning);
    }

    .qlb-passage-title {
        font-size: .74rem;
        font-weight: 800;
        color: var(--q-warning);
        margin-bottom: .35rem;
        display: flex;
        align-items: center;
        gap: .35rem;
    }

    .qlb-passage-text {
        font-size: .84rem;
        color: #374151;
        line-height: 1.7;
        max-height: 140px;
        overflow-y: auto;
        font-weight: 600;
    }

        .qlb-passage-text::-webkit-scrollbar {
            width: 4px;
        }

        .qlb-passage-text::-webkit-scrollbar-thumb {
            background: #d1c89a;
            border-radius: 99px;
        }

    .qlb-media-wrap {
        background: #f0f9ff;
        border: 1px solid #bae6fd;
        border-radius: 10px;
        padding: .6rem .75rem;
        border-right: 3px solid var(--q-info);
    }

    .ltr-mode .qlb-media-wrap {
        border-right: 1px solid #bae6fd;
        border-left: 3px solid var(--q-info);
    }

    .qlb-media-label {
        font-size: .74rem;
        font-weight: 800;
        color: var(--q-info);
        margin-bottom: .4rem;
        display: flex;
        align-items: center;
        gap: .35rem;
    }

    .qlb-media-wrap audio,
    .qlb-media-wrap video {
        width: 100%;
        border-radius: 10px;
        outline: none;
    }

    .qlb-media-wrap video {
        max-height: 260px;
        background: #000;
    }

    .qlb-compare {
        display: grid;
        grid-template-columns: 1fr auto 1fr;
        gap: .5rem;
        align-items: center;
    }

    .qlb-cmp-box {
        background: var(--q-primary-lt);
        border: 1px solid #c4b5fd;
        border-radius: 10px;
        padding: .55rem .7rem;
        text-align: center;
        font-size: .86rem;
        font-weight: 800;
        color: var(--q-primary);
        display: flex;
        align-items: center;
        justify-content: center;
        line-height: 1.5;
    }

    .qlb-cmp-vs {
        font-size: .68rem;
        font-weight: 800;
        color: var(--q-muted);
        background: var(--q-border);
        border-radius: 99px;
        padding: .15rem .5rem;
        white-space: nowrap;
    }

    .qlb-qimg {
        border-radius: 10px;
        overflow: hidden;
        border: 1px solid var(--q-border);
        text-align: center;
        background: #fff;
        padding: .4rem;
    }

        .qlb-qimg img {
            max-width: 100%;
            max-height: 200px;
            object-fit: contain;
            display: block;
            margin: 0 auto;
            border-radius: 8px;
        }

    .qlb-qtitle {
        font-size: .93rem;
        color: var(--q-text);
        line-height: 1.7;
        font-weight: 800;
        background: #ffffff;
        border: 1px solid var(--q-border);
        border-right: 3px solid var(--q-primary);
        border-radius: 10px;
        padding: .65rem .8rem;
    }

    .ltr-mode .qlb-qtitle {
        border-right: 1px solid var(--q-border);
        border-left: 3px solid var(--q-primary);
    }

    .qlb-qtitle strong,
    .qlb-qtitle p,
    .qlb-qtitle span,
    .qlb-qtitle div {
        font-weight: 700 !important;
    }

    .qlb-qtitle img {
        max-height: 70px;
        max-width: 100%;
        border-radius: 6px;
        vertical-align: middle;
    }

    .qlb-qtitle p {
        margin: 0;
    }

    .qlb-options {
        display: flex;
        flex-direction: column;
        gap: .4rem;
    }

    .qlb-opt {
        border-radius: 9px;
        border: 1.5px solid var(--q-border);
        padding: .5rem .7rem;
        display: flex;
        align-items: center;
        gap: .55rem;
        font-size: .84rem;
        background: #fff;
        transition: border-color .15s, background .15s;
    }

        .qlb-opt.opt-correct {
            border-color: var(--q-success);
            background: var(--q-success-lt);
        }

        .qlb-opt.opt-wrong {
            border-color: var(--q-danger);
            background: var(--q-danger-lt);
        }

        .qlb-opt.opt-normal {
            border-color: var(--q-border);
            background: #fff;
        }

    .qlb-opt-label {
        width: 24px;
        height: 24px;
        border-radius: 6px;
        flex-shrink: 0;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: .74rem;
        font-weight: 800;
        background: #d1d5db;
        color: #fff;
    }

    .opt-correct .qlb-opt-label {
        background: var(--q-success);
        color: #fff;
    }

    .opt-wrong .qlb-opt-label {
        background: var(--q-danger);
        color: #fff;
    }

    .qlb-opt-content {
        flex: 1;
        min-width: 0;
    }

    .qlb-opt-text {
        line-height: 1.5;
        color: var(--q-text);
        font-weight: 700;
    }

        .qlb-opt-text img {
            max-height: 50px;
            max-width: 100%;
            border-radius: 6px;
            vertical-align: middle;
        }

        .qlb-opt-text p {
            margin: 0;
        }

    .qlb-opt-img {
        max-height: 56px;
        max-width: 160px;
        object-fit: contain;
        border-radius: 7px;
        border: 1px solid var(--q-border);
        background: #fff;
        padding: .15rem;
        margin-bottom: .3rem;
        display: block;
    }

    .qlb-opt-tags {
        display: flex;
        align-items: center;
        gap: .3rem;
        flex-wrap: wrap;
        margin-top: .3rem;
    }

    .qlb-mini-badge {
        border-radius: 99px;
        padding: .14rem .5rem;
        font-size: .66rem;
        font-weight: 800;
        display: inline-flex;
        align-items: center;
        gap: .22rem;
    }

    .badge-student {
        background: var(--q-info-lt);
        color: var(--q-info);
        border: 1px solid #67e8f9;
    }

    .badge-correct {
        background: var(--q-success);
        color: #fff;
        border: 1px solid #15803d;
    }

    .qlb-opt-icon {
        font-size: .9rem;
        flex-shrink: 0;
        margin-right: auto;
    }

    .ltr-mode .qlb-opt-icon {
        margin-right: 0;
        margin-left: auto;
    }

    .opt-correct .qlb-opt-icon {
        color: var(--q-success);
    }

    .opt-wrong .qlb-opt-icon {
        color: var(--q-danger);
    }

    .opt-normal .qlb-opt-icon {
        color: #9ca3af;
    }

    .qlb-result-box {
        border-radius: 10px;
        padding: .55rem .75rem;
        border: 1px solid var(--q-border);
        display: flex;
        align-items: center;
        justify-content: space-between;
        gap: .6rem;
        flex-wrap: wrap;
    }

        .qlb-result-box.is-correct {
            background: var(--q-success-lt);
            border-color: #86efac;
            border-right: 3px solid var(--q-success);
        }

        .qlb-result-box.is-wrong {
            background: var(--q-danger-lt);
            border-color: #fecaca;
            border-right: 3px solid var(--q-danger);
        }

        .qlb-result-box.is-skip {
            background: var(--q-skip-lt);
            border-color: #d1d5db;
            border-right: 3px solid var(--q-skip);
        }

    .ltr-mode .qlb-result-box.is-correct,
    .ltr-mode .qlb-result-box.is-wrong,
    .ltr-mode .qlb-result-box.is-skip {
        border-right-width: 1px;
        border-left-width: 3px;
    }

    .qlb-result-title {
        font-size: .78rem;
        font-weight: 800;
        color: var(--q-text);
        display: inline-flex;
        align-items: center;
        gap: .35rem;
    }

    .qlb-result-value {
        font-size: .82rem;
        font-weight: 800;
    }

        .qlb-result-value.text-ok {
            color: var(--q-success);
        }

        .qlb-result-value.text-bad {
            color: var(--q-danger);
        }

        .qlb-result-value.text-skip {
            color: var(--q-skip);
        }

    .qlb-explanation-wrap {
        background: #fafafa;
        border: 1px solid var(--q-border);
        border-radius: 12px;
        padding: .6rem;
    }

    .qlb-card-foot {
        background: #fafafa;
        border-top: 1px solid var(--q-border);
        padding: .5rem .8rem;
        display: flex;
        align-items: center;
        gap: .4rem;
        flex-wrap: wrap;
    }

    .qlb-foot-chip {
        display: inline-flex;
        align-items: center;
        gap: .28rem;
        font-size: .72rem;
        font-weight: 800;
        padding: .18rem .55rem;
        border-radius: 99px;
    }

    .fc-primary {
        background: var(--q-primary-lt);
        color: var(--q-primary);
    }

    .fc-success {
        background: var(--q-success-lt);
        color: var(--q-success);
    }

    .fc-danger {
        background: var(--q-danger-lt);
        color: var(--q-danger);
    }

    .fc-skip {
        background: var(--q-skip-lt);
        color: var(--q-skip);
    }

    .fc-time {
        background: var(--q-warning-lt);
        color: var(--q-warning);
        margin-right: auto;
    }

    .ltr-mode .fc-time {
        margin-right: 0;
        margin-left: auto;
    }

    .qlb-empty {
        border: 1px dashed var(--q-border);
        background: var(--q-skip-lt);
        color: var(--q-muted);
        border-radius: 14px;
        padding: 1.5rem;
        text-align: center;
        font-size: .9rem;
        font-weight: 900;
    }

    @@media (max-width: 992px) {
        .qlb-stats {
            grid-template-columns: repeat(2, 1fr);
        }

        .qlb-grid {
            grid-template-columns: 1fr;
        }
    }

    @@media (max-width: 576px) {
        .qlb-page {
            padding: 1rem .7rem 3rem;
        }

        .qlb-hero {
            padding: 1.5rem 1.1rem;
        }

        .qlb-stats {
            grid-template-columns: 1fr;
        }

        .qlb-card-foot {
            align-items: stretch;
        }

        .qlb-foot-chip {
            width: 100%;
            justify-content: center;
        }

        .qlb-time-chip {
            width: 100%;
            margin-right: 0;
            margin-left: 0;
        }

        .fc-time {
            margin-right: 0;
            margin-left: 0;
        }

        .qlb-compare {
            grid-template-columns: 1fr;
        }

        .qlb-cmp-vs {
            display: none;
        }
    }
</style>
