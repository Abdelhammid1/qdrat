@model QdratNew.ViewModels.AI.ChartAnalysisInputViewModel
@using System.Text.Json

<button type="button"
        class="btn btn-sm btn-outline-light float-end"
        title="تحليل ذكي"
        onclick='openSmartAnalysisModal("@Model.ChartId", "@Model.Title", @Html.Raw(JsonSerializer.Serialize(Model.DataPoints)))'>
    <i class="fas fa-brain"></i>
</button>
