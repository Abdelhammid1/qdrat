@model int

@if (Model > 0)
{
    <span class="badge rounded-pill bg-danger ms-auto" title="طلبات جديدة لم يتم التواصل معها">
        <span class="visually-hidden">طلبات جديدة: </span>@(Model > 99 ? "99+" : Model.ToString())
    </span>
}
